spring:
  application:
    name: gateway

server:
  port: ${port:8080}

zuul:
  routes:
    resources:
      path: /api/v2/**
      serviceId: api-v2

hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds: 100