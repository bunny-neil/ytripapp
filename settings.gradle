rootProject.name = 'ytripapp'
include 'migration'
include 'api-common'
include 'api'
include 'api-client'
include 'gateway'

rootProject.children.each { child ->
    child.buildFileName = "${child.name}.gradle"
}


